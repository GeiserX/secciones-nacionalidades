version: 2
updates:
- package-ecosystem: docker
  directory: /
  schedule:
    interval: weekly
    day: monday
  commit-message:
    prefix: docker
  labels:
  - docker
  - automated
  groups:
    docker_security:
      applies-to: security-updates
      patterns:
      - '*'
