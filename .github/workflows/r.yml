# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
#
# See https://github.com/r-lib/actions/tree/master/examples#readme for
# additional example workflows available for the R community.

name: Test and Deployment to K3s cluster

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: 	ubuntu-latest
#    strategy:
#      matrix:
#        r-version: [3.6]

    steps:
      - uses: actions/checkout@v2
        
#      - name: Install dependencies
#        run: |
#          install.packages(c("remotes", "rcmdcheck"))
#          remotes::install_deps(dependencies = TRUE)
#        shell: Rscript {0}
#      - name: Test WebApp
#        run: rcmdcheck::rcmdcheck(args = "--no-manual", error_on = "error")
#        shell: Rscript {0}
     
      - name: Build and Push Dockerfile
        run: |
          echo $docker_password | docker login -u drumsergio --password-stdin
          docker build -t drumsergio/secciones-nacionalidades:auto-latest -f Dockerfile
          docker push drumsergio/secciones-nacionalidades:auto-latest
          
      - name: Deploy to Kubernetes
        uses: steebchen/kubectl@master
        env:
          KUBE_CONFIG_DATA: ${{ secrets.kubeconfig_data }}
          KUBECTL_VERSION: "1.18"
        with:
          args: set image --record deployment/sec-nac container=${{ github.repository }}:auto-latest

          


